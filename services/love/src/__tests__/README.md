# Tests for love service

See `docs/love.md`, section 11 (Тесты) - test layout, helpers, mocking strategy
and the `parseRoomName` / `webhook.test.ts` gotchas live there.

```bash
rushx test
rushx test --coverage
npx jest webhook.test.ts
```
