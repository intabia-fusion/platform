# Generated by gen-profile-overlay.js - do not edit, ./prepare-pg.sh --profile rewrites it.
#
# Runs every Node pod under V8's sampling CPU profiler. Profiles are flushed on SIGTERM into
# ./profiles/<service>/ - collect with ./profile-collect.sh, then ./profile-report.sh.
#
# Swap the profiler with PROFILE_NODE_FLAGS, e.g.
#   PROFILE_NODE_FLAGS='--heap-prof --heap-prof-dir' ./prepare-pg.sh --profile
# The service's own directory is appended to the last flag.
x-prof-volumes: &prof-volumes
  - ./profiles:/profiles
  - ./profile-preload.js:/profile-preload.js:ro

services:
  account:
    environment:
      - PROFILE_DIR=/profiles/account
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/account --require /profile-preload.js
    volumes: *prof-volumes
  activity:
    environment:
      - PROFILE_DIR=/profiles/activity
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/activity --require /profile-preload.js
    volumes: *prof-volumes
  analytics:
    environment:
      - PROFILE_DIR=/profiles/analytics
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/analytics --require /profile-preload.js
    volumes: *prof-volumes
  billing:
    environment:
      - PROFILE_DIR=/profiles/billing
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/billing --require /profile-preload.js
    volumes: *prof-volumes
  collaborator0:
    environment:
      - PROFILE_DIR=/profiles/collaborator0
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/collaborator0 --require /profile-preload.js
    volumes: *prof-volumes
  datalake:
    environment:
      - PROFILE_DIR=/profiles/datalake
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/datalake --require /profile-preload.js
    volumes: *prof-volumes
  front0:
    environment:
      - PROFILE_DIR=/profiles/front0
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/front0 --require /profile-preload.js
    volumes: *prof-volumes
  fulltext:
    environment:
      - PROFILE_DIR=/profiles/fulltext
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/fulltext --require /profile-preload.js
    volumes: *prof-volumes
  love:
    environment:
      - PROFILE_DIR=/profiles/love
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/love --require /profile-preload.js
    volumes: *prof-volumes
  notifications:
    environment:
      - PROFILE_DIR=/profiles/notifications
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/notifications --require /profile-preload.js
    volumes: *prof-volumes
  payment:
    environment:
      - PROFILE_DIR=/profiles/payment
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/payment --require /profile-preload.js
    volumes: *prof-volumes
  preview:
    environment:
      - PROFILE_DIR=/profiles/preview
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/preview --require /profile-preload.js
    volumes: *prof-volumes
  print:
    environment:
      - PROFILE_DIR=/profiles/print
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/print --require /profile-preload.js
    volumes: *prof-volumes
  rekoni:
    environment:
      - PROFILE_DIR=/profiles/rekoni
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/rekoni --require /profile-preload.js
    volumes: *prof-volumes
  sign:
    environment:
      - PROFILE_DIR=/profiles/sign
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/sign --require /profile-preload.js
    volumes: *prof-volumes
  stats:
    environment:
      - PROFILE_DIR=/profiles/stats
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/stats --require /profile-preload.js
    volumes: *prof-volumes
  stream:
    environment:
      - PROFILE_DIR=/profiles/stream
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/stream --require /profile-preload.js
    volumes: *prof-volumes
  tbank-subscriptions:
    environment:
      - PROFILE_DIR=/profiles/tbank-subscriptions
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/tbank-subscriptions --require /profile-preload.js
    volumes: *prof-volumes
  time-machine:
    environment:
      - PROFILE_DIR=/profiles/time-machine
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/time-machine --require /profile-preload.js
    volumes: *prof-volumes
  transactor0:
    environment:
      - PROFILE_DIR=/profiles/transactor0
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/transactor0 --require /profile-preload.js
    volumes: *prof-volumes
  workspace:
    environment:
      - PROFILE_DIR=/profiles/workspace
      - NODE_OPTIONS=${PROFILE_NODE_FLAGS:---cpu-prof --cpu-prof-dir}=/profiles/workspace --require /profile-preload.js
    volumes: *prof-volumes
